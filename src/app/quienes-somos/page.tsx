import {
  createPageMetadata,
  PagePlaceholder,
} from "@/components/shared/PagePlaceholder";

export const metadata = createPageMetadata("Quiénes somos");

export default function QuienesSomosPage() {
  return <PagePlaceholder title="Quiénes somos" />;
}
