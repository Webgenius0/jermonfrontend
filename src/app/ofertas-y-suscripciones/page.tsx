import {
  createPageMetadata,
  PagePlaceholder,
} from "@/components/shared/PagePlaceholder";

export const metadata = createPageMetadata("Ofertas y Suscripciones");

export default function OfertasPage() {
  return <PagePlaceholder title="Ofertas y Suscripciones" />;
}
