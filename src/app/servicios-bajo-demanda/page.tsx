import {
  createPageMetadata,
  PagePlaceholder,
} from "@/components/shared/PagePlaceholder";

export const metadata = createPageMetadata("Servicios bajo demanda");

export default function ServiciosBajoDemandaPage() {
  return <PagePlaceholder title="Servicios bajo demanda" />;
}
